import { translations } from 'locales/translations';
import { _t } from 'utils/messages';

export const messages = {
  funders: () => _t(translations.header.funders),
  projectHolders: () => _t(translations.header.projectHolders),
  showcase: () => _t(translations.header.showcase),
};
